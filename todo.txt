- anv. c11 typedef redef ext? ge typer _t suffix, kanske ha macro för fwd declare (lägg i types.h och döp om till global_include.h)? typ fwd(renderer_state_t)
- dra in stdint, stdbool i types istället för min skit i types.h (global_include.h)? återgå till size_t? (dra även in stdlib)
- var nöjd med ovanstående
- fixa shader load